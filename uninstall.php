<?php
/**
 * Fired when the plugin is uninstalled.
 *
 * @package HikmahLogin
 */

// Exit if uninstall not called from WordPress.
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}

// Placeholder: clean up plugin data on uninstall.
// delete_option( 'hikmah_login_options' );
